# AIL Pipeline Language Specification

> The spec has moved to `spec/core/`. See [spec/README.md](spec/README.md) for the navigation index.

To read the full spec as a single document:

```bash
cat spec/core/s*.md
```
