# AIL Runner Specification

> The runner spec has moved to `spec/runner/`. See [spec/README.md](spec/README.md) for the navigation index.

To read the full runner spec as a single document:

```bash
cat spec/runner/r*.md
```
