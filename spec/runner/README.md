# AIL Runner Contract Specification — Section Index

This directory contains the AIL Runner Specification split into per-section files.

See [../README.md](../README.md) for the full navigation index with one-line summaries and implementation status per section.

To reassemble the full runner spec as a single document:

```bash
cat spec/runner/r*.md
```
