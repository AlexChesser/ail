# AIL Pipeline Language Specification — Section Index

This directory contains the core AIL Pipeline Language Specification split into per-section files.

See [../README.md](../README.md) for the full navigation index with one-line summaries and implementation status per section.

To reassemble the full spec as a single document:

```bash
cat spec/core/s*.md
```
